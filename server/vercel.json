{
  "version": 2,
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/",
      "methods": ["GET", "PUT", "POST", "DELETE", "PATCH", "OPTIONS"],
      "header": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
